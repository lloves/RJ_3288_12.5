1. 蓝牙“屡次停止运行”的问题

    /device/rockchip/common/manifest.xml文件修改bluetooth部分并不能屏蔽蓝牙功能；
     
     BOARD_BLUETOOTH_SUPPORT ?= false
     BOARD_BLUETOOTH_LE_SUPPORT ?= false
     修改bluetooth部分并不能屏蔽蓝牙功能；
    
    
    关键在：hardware/realtek/rtkbt/rtkbt.mk中并没有判断，所以rk编译脚本逻辑比较凌乱，这里需要控制下。
    补丁：
    diff --git a/rtkbt/rtkbt.mk b/rtkbt/rtkbt.mk
      index 41d258c..b9d9906 100644
      --- a/rtkbt/rtkbt.mk
      +++ b/rtkbt/rtkbt.mk
      @@ -1,10 +1,12 @@
       # RELEASE NAME: 20171107_BT_ANDROID_8.x

      -BOARD_HAVE_BLUETOOTH := true
      +# BOARD_HAVE_BLUETOOTH := true
       BOARD_HAVE_BLUETOOTH_RTK := true
       BOARD_HAVE_BLUETOOTH_RTK_COEX := true


      +ifeq ($(BOARD_HAVE_BLUETOOTH),true)
      +
       BOARD_BLUETOOTH_BDROID_BUILDCFG_INCLUDE_DIR := $(LOCAL_PATH)/bluetooth


      @@ -87,4 +89,4 @@ PRODUCT_PROPERTY_OVERRIDES += \
              persist.bluetooth.rtkcoex=true \
              bluetooth.enable_timeout_ms=11000

      -
      +endif
